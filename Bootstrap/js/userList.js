// 加载首页数据
$.ajax({
    url:
    type:
    data:{
        type:'page',
        page:1,
    },
    dataType:'json',
    success:function(json){
        console.log( json );
        // 生成分页

        // 第一页设置为激活状态
        

        // 展示数据
    }
})

// 页码下标
var index = 0;

// 选择页码 事件委托 父级绑定点击事件
$('').on('click','',function(){


})

// 编辑事件

// 提交事件
